# 🛍️ DigiShop | پلتفرم فروشگاهی چند فروشنده (Multi-Vendor)

**دیجی‌شاپ** یک پلتفرم تجارت الکترونیک مدرن، مقیاس‌پذیر و پیشرفته است که با الهام از دیجی‌کالا و با استفاده از قدرت **Django 5** و **Tailwind CSS** توسعه یافته است.
این پروژه صرفاً یک فروشگاه ساده نیست؛ بلکه تمرکز اصلی بر روی **معماری تمیز (Clean Architecture)**، **پرفورمنس بالا** و **تجربه کاربری (UX) بدون رفرش** با استفاده از HTMX است.

![Project Status](https://img.shields.io/badge/Status-Active_Development-green)
![Python](https://img.shields.io/badge/Python-3.12+-blue)
![Django](https://img.shields.io/badge/Django-5.2_LTS-092E20)
![Tailwind](https://img.shields.io/badge/Tailwind_CSS-v4-38B2AC)

---

## ✨ ویژگی‌های کلیدی و دستاوردهای فنی

### 🏗️ معماری و هسته سیستم
* **Django 5.2 LTS:** استفاده از جدیدترین نسخه پایدار جنگو با قابلیت‌های Async.
* **PostgreSQL:** استفاده از دیتابیس قدرتمند پستگرس و قابلیت‌های `JSONField` برای ذخیره‌سازی داده‌های منعطف.
* **Clean Code:** رعایت اصول SOLID و تفکیک منطقی اپلیکیشن‌ها (Core, Accounts, Products, Orders).

### 🛒 فروشگاه و محصولات (Advanced Catalog)
* **سیستم چند فروشندگی (Multi-Vendor):** قابلیت فروش یک محصول توسط چندین فروشنده مختلف.
* **ویژگی‌های محصول داینامیک (EAV Pattern):** استفاده از `JSONField` برای تعریف ویژگی‌های متفاوت برای هر دسته‌بندی (مثلاً رم برای لپ‌تاپ، جنس پارچه برای لباس).
* **گالری تصاویر پیشرفته:** نمایش تصاویر با قابلیت بزرگنمایی و تعویض سریع با **Alpine.js**.
* **سیستم نقد و بررسی:** ثبت امتیاز، نظر، لایک/دیس‌لایک نظرات و پیشنهاد خرید (مشابه دیجی‌کالا) با آپدیت **HTMX**.

### ⚡ تعاملات مدرن (HTMX & Alpine.js)
* **سبد خرید بدون رفرش (SPA-Like):** افزودن، حذف و تغییر تعداد محصول در سبد خرید بدون رفرش صفحه با استفاده از تکنولوژی **HTMX**.
* **مینی‌کارت (Mini Cart):** نمایش خلاصه سبد خرید در هدر با هاور کردن.
* **لیست علاقه‌مندی‌ها (Wishlist):** افزودن به علاقه‌مندی‌ها به صورت آنی (AJAX).

### 🔐 کاربران و امنیت
* **ورود دو مرحله‌ای (OTP):** سیستم احراز هویت با شماره موبایل و کد تایید پیامکی (متصل به وب‌سرویس ملی پیامک).
* **داشبورد کاربری حرفه‌ای:** مدیریت پروفایل، آدرس‌ها (CRUD کامل)، مشاهده تاریخچه سفارش‌ها و لیست علاقه‌مندی‌ها.
* **مدل کاربر سفارشی (Custom User Model):** جایگزینی Username با شماره موبایل.

### 📦 مدیریت سفارشات و انبارداری (Inventory Management)
* **انبارداری دقیق و همزمان (Concurrency Safe):** استفاده از `select_for_update` و `transaction.atomic` برای جلوگیری از Race Condition در لحظه پرداخت (جلوگیری از فروش بیش از موجودی).
* **مدیریت هوشمند موجودی (Signals):** بازگشت خودکار موجودی در صورت لغو یا مرجوع شدن سفارش توسط سیگنال‌های جنگو.
* **درگاه پرداخت واقعی (Sandbox):** پیاده‌سازی کامل چرخه پرداخت با درگاه **زرین‌پال** (درخواست، هدایت، وریفای).
* **فاکتور Snapshot:** ذخیره قیمت محصول در لحظه خرید (تغییر قیمت بعدی محصول تاثیری روی فاکتورهای قبل ندارد).

### 🎨 رابط کاربری و پنل مدیریت
* **Tailwind CSS v4:** طراحی کاملاً ریسپانسیو و کاستوم.
* **پنل ادمین مدرن:** شخصی‌سازی کامل پنل ادمین جنگو با استفاده از پکیج **Django-Unfold**.

---

## 🛠️ تکنولوژی‌های مورد استفاده (Tech Stack)

| بخش | تکنولوژی | توضیحات |
| --- | --- | --- |
| **Backend** | Python, Django 5.2 | فریم‌ورک اصلی |
| **Database** | PostgreSQL | دیتابیس رابطه‌ای + JSON |
| **Frontend** | HTML5, Tailwind CSS | استایل‌دهی مدرن |
| **Interactions** | HTMX, Alpine.js | تعاملات سمت کلاینت بدون فریم‌ورک‌های سنگین JS |
| **Admin Panel** | Django-Unfold | تم مدرن برای پنل مدیریت |
| **Payment** | ZarinPal API | اتصال مستقیم به درگاه پرداخت |
| **SMS** | Melipayamak API | سرویس ارسال پیامک OTP |

---

## 🚀 راهنمای نصب و اجرا (Installation)

برای اجرای این پروژه روی سیستم خود مراحل زیر را دنبال کنید:

1.  **کلون کردن مخزن:**
    ```bash
    git clone [https://github.com/m-nazarian/DigiShop-Django-MultiVendor.git](https://github.com/m-nazarian/DigiShop-Django-MultiVendor.git)
    cd DigiShop-Django-MultiVendor
    ```

2.  **ساخت محیط مجازی (Virtual Environment):**
    ```bash
    python -m venv .venv
    # ویندوز:
    .venv\Scripts\activate
    # مک/لینوکس:
    source .venv/bin/activate
    ```

3.  **نصب پیش‌نیازها:**
    ```bash
    pip install -r requirements.txt
    ```

4.  **تنظیم متغیرهای محیطی:**
    یک فایل `.env` در ریشه پروژه بسازید و مقادیر زیر را تنظیم کنید:
    ```env
    SECRET_KEY=your_secret_key
    DEBUG=True
    DB_NAME=digishop_db
    DB_USER=postgres
    DB_PASSWORD=your_db_password
    MELIPAYAMAK_USERNAME=your_sms_user
    MELIPAYAMAK_PASSWORD=your_sms_pass
    ```

5.  **نصب وابستگی‌های Tailwind (نیاز به Node.js):**
    ```bash
    python manage.py tailwind install
    ```

6.  **ساخت دیتابیس و اجرا:**
    ```bash
    python manage.py migrate
    python manage.py createsuperuser
    ```

7.  **اجرای سرور و کامپایلر استایل:**
    در ترمینال ۱:
    ```bash
    python manage.py tailwind start
    ```
    در ترمینال ۲:
    ```bash
    python manage.py runserver
    ```

---

## 🔮 برنامه توسعه آینده (Roadmap)

- [ ] افزودن **Redis** برای کشینگ و افزایش سرعت.
- [ ] پیاده‌سازی **Celery** برای پردازش‌های پس‌زمینه (ارسال پیامک انبوه، گزارش‌گیری).
- [ ] سیستم جستجوی پیشرفته (Full-Text Search) با پستگرس.
- [ ] مقایسه محصولات پیشرفته.
- [ ] داکرایز کردن پروژه (Docker & Docker Compose).

---

**توسعه داده شده با ❤️ توسط [محمد نظریان]**